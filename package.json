{
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "db:push": "pnpm --filter @reeva/db prisma db push",
    "db:studio": "pnpm --filter @reeva/db prisma studio",
    "worker": "pnpm --filter @reeva/worker dev"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
